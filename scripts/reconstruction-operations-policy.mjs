export const phases=["emergency","transition","reconstruction"];
export const freshnessHours={emergency:6,school:72,reconstruction:336,permanent:2160};
export const validatePhase=value=>phases.includes(value);
export const linkDecision=({consecutiveFailures=0,status=200,redirect=false,redirected=false}={})=>({status:redirect||redirected?"WARNING":status===404&&consecutiveFailures>=2?"ACTION_REQUIRED":status===404||status>=500?"WARNING":"OK"});
export const sourceDecision=({baselineHash,previousHash,currentHash,verified=false,verificationStatus}={})=>{const changed=(baselineHash||previousHash)&&currentHash&&(baselineHash||previousHash)!==currentHash;return changed&&(verified||verificationStatus==="verified")?{status:"ACTION_REQUIRED",verificationStatus:"needs_review"}:{status:"OK",verificationStatus:verificationStatus||null};};
export const freshnessDecision=(phaseOrOptions,published)=>{const options=typeof phaseOrOptions==="string"?{informationPhase:phaseOrOptions,publishedAt:published}:phaseOrOptions||{};const {informationPhase="reconstruction",publishedAt,now=Date.now()}=options;if(!publishedAt)return{state:"unknown",status:"WARNING"};const age=(now-Date.parse(publishedAt))/36e5;const limit=informationPhase==="emergency"?freshnessHours.emergency:freshnessHours.reconstruction;return{state:age>limit?"stale":"fresh",status:age>limit?"WARNING":"OK",ageHours:Math.round(age),limitHours:limit};};
