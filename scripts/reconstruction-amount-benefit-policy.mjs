export const BENEFIT_LABELS=Object.freeze({grant:{label:"返済不要の給付",notice:"対象条件の確認が必要です。"},loan:{label:"返済が必要な貸付",notice:"返済条件を公式情報で確認してください。"},reduction:{label:"負担を軽くする減額",notice:"現金を受け取る制度ではありません。"},exemption:{label:"支払いを免除する制度",notice:"対象となる税・料金と条件を確認してください。"},deferral:{label:"支払い時期を延ばす制度",notice:"支払いが免除される制度ではありません。"},in_kind:{label:"品物・サービスによる支援",notice:"本人への現金給付ではありません。"},direct_payment:{label:"実施主体から業者等への直接支払い",notice:"本人への現金給付ではありません。"},housing:{label:"住まいを提供する支援",notice:"金額による給付とは限りません。"},consultation:{label:"相談・手続き支援",notice:"相談先で状況を確認します。"},guarantee:{label:"融資等の保証",notice:"直接現金を受け取る制度ではありません。"},other:{label:"その他の支援",notice:"支援方式を公式情報で確認してください。"}});
export const UNIT_LABELS=Object.freeze({per_household:"1世帯あたり",per_person:"1人あたり",per_case:"1件あたり",per_home:"1住宅あたり",individual_calculation:"個別計算",not_applicable:"金額の支援ではありません",unknown:"単位を確認中"});
export function isPublicAmount(item,program,application,links,sources){if(!item||!program||item.publicationStatus!=="published"||item.verificationStatus!=="verified"||item.freshnessStatus!=="fresh"||item.certainty!=="confirmed"||!item.checkedAt)return false;if(item.applicationId&&!application)return false;if(item.applicationId&&application.publicationStatus!=="published")return false;if(program.publicationStatus!=="published"||program.verificationStatus!=="verified"||program.benefitType!==item.benefitType)return false;return (item.sourceLinkIds||[]).some(id=>{const link=links.get(id),source=link&&sources.get(link.sourceId);return link?.claimType==="amount"&&link.verifiedAt&&source?.status==="active"});}
export function formatAmount(item){if(item.amount===null)return null;return `${new Intl.NumberFormat("ja-JP").format(item.amount)}円`;}
export function publicAmount(item){return{id:item.id,amountType:item.amountType,formattedAmount:formatAmount(item),unitLabel:UNIT_LABELS[item.unit],taxTreatment:item.taxTreatment,rate:item.rate,description:item.description,notice:item.amountType==="maximum"?"上限額であり、全員がこの額になるわけではありません。":"条件により内容が異なります。",checkedAt:item.checkedAt};}
export function consistencyErrors(item,program){const text=`${item.description||""} ${program?.shortDescription||""}`;const errors=[];if(item.benefitType==="loan"&&/返済不要|返す必要[はが]ない/.test(text))errors.push("loan/grant説明矛盾");if(item.benefitType==="grant"&&/返済が必要|貸付/.test(text))errors.push("grant/loan説明矛盾");if(["reduction","exemption","deferral","in_kind","direct_payment","guarantee"].includes(item.benefitType)&&/現金(?:を|が)?(?:支給|給付)(?:します|されます|する制度です)/.test(text))errors.push("非現金支援を現金給付と表現");return errors;}
